Geometric nine-point star (the primary mark) with optional construction circle.

```jsx
<Nonagram size={240} weight="subject" />
<Nonagram size={48} showConstruction={false} />
```

Use `weight="subject"` only when the nonagram is the subject of the sheet.
